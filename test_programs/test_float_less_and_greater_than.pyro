func main() {
    let a = 3.0;
    let b = 5.2;
    let c = 3.0;

    print("{float} < {float} => {boolean}", a, b, a < b);
    print("{float} > {float} => {boolean}", a, b, a > b);
    print("{float} > {float} => {boolean}", a, c, a > c);
}
